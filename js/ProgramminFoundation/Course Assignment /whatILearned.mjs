parseInt(); // Takes string and turns to rounded number
parseFloat(); //Takes string and turns to number keeping decimals
Math.ceil(); //Takes number and rounds up to nearest integer
Math.floor(); //Takes number and rounds down to nearest integer
//ANY 0-1 number 0.2 0.4
Math.random(); //Generated random number from 0-1
Math.round(); //Rounds up or down to nearest integer

string.split(separator, limit); //splits string into an array
//Example
let text = 'Hello world from JavaScript';
text.split(' '); // ["Hello", "world", "from", "JavaScript"]

text.substring(startIndex, endIndex); // get what is in the string from index from to

//Can also change to rounded number at once
parseInt(prompt('How old are you?'));

confirm('Text'); //returns true or false

arrayName.find(); //First first array element that matches

arrayName.indexOf(); // takes array and gives first match

//Array of words, index of word +1, both it and next word replace by word
array.splice(wordIndex + 1, 2, firstWord, firstWord);

sentence.split(''); //split string by letters
sentence.split(' '); //split string by words
array.slice(); //Copies from index up to
array.splice(); //Removes and or replaces indexes with other value


1.Üb 2D arrays 
2.Ûb von string ins array zu tausche
3.Ûb aus den String sachen zu holen und tauschen